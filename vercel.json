{
  "version": 2,
  "builds": [
    {
      "src": "back-end/server.js",
      "use": "@vercel/node",
      "config": { 
        "includeFiles": ["data/**"]  // Explicitly include JSON data
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "back-end/server.js" }
  ]
}